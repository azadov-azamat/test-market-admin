import Router from './router'

const App = () => {

    // const dispatch = useDispatch()
    // const auth = useSelector(state => state.auth)
    //
    // useEffect(() => {
    //     if (!!auth.accessToken) {
    //         dispatch(loadUser())
    //     }
    // }, [auth.accessToken])

    return <Router/>
}

export default App